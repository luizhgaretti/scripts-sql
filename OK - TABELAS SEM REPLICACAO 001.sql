alter table sljemp2 add ntpsints numeric(1,0) default 0 not null

go

alter table sljemp2 add ultnsunfes numeric(15,0) default 0 not null

go

alter table sljemp2 add cfoisenefd char(10) collate SQL_Latin1_General_CP1_CI_AS default ' ' not null

go

alter table sljempc2 add ntpsints numeric(1,0) default 0 not null

go

alter table sljempc2 add ultnsunfes numeric(15,0) default 0 not null

go

alter table sljempc2 add cfoisenefd char(10) collate SQL_Latin1_General_CP1_CI_AS default ' ' not null

go